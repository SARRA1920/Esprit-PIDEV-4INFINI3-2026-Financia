/** Fichier minimal pour que `tsconfig.spec.json` ait des entrées (TS18003) et que `ng test` puisse démarrer. */
describe('Financia Angular', () => {
  it('charge la config de tests', () => {
    expect(true).toBe(true);
  });
});
